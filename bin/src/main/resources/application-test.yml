spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: 
  profile: test
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true