server:
  port: 8889
  compression:
      enabled: true
  servlet:
    context-path: /api
spring:
  application:
    name: dealership
  profiles:
    active: dev
  datasource:
    servlet:
      multipart:
        enabled: true
        file-size-threshold: 2KB
        max-file-size: 200MB
        max-request-size: 215MB
file:
  upload-dir: /home/runner/work/Spring-Dealership/UploadDir
api:
  security:
    token:
      secret: i9a(Pj7@39&
spring-doc:
  pathsToMatch: /**
  swagger-ui:
    use-root-path: true
management:
  endpoints:
    web:
      exposure:
        include: health
      base-path: /actuator
  endpoint:
    health:
      show-details: always